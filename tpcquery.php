<?php 
    // Redirect to server that have the results of the query
    // $sid = $_GET['sid'];
    // $loc = 'http://79.125.5.22'; //$_GET['loc'];
    // if (!ctype_digit($sid)) return;
    // $path_loc = $loc.'/tpc/var/'.$sid.'.html';
    // In an Ajax request the header(...) function does not work
    // echo file_get_contents( $path_loc );
    //echo $path_loc;

